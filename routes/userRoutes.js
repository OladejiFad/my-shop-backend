
/*const express = require('express');
const router = express.Router();
const { updateShopName, updateShopTheme, updateShopColor, getShopCustomization } = require('../controllers/userController');
const { verifyToken } = require('../middleware/authMiddleware');


// Get current shop customization
router.get('/shop/customization', verifyToken, getShopCustomization);

// Update shop name
router.put('/shop/name', verifyToken, updateShopName);

// Update shop theme
router.put('/shop/theme', verifyToken, updateShopTheme);

// Update shop color
router.put('/shop/color', verifyToken, updateShopColor);


module.exports = router; */
